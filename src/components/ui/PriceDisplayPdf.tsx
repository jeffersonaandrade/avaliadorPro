"use client";

/**
 * PDF: mesmas regras que `PriceDisplay` / `PriceHero` (sem exceção de formatação).
 */
export {
  PriceHero as PriceDisplayPdf,
  PriceInline,
} from "@/components/ui/PriceDisplay";
